# path to root directory
# PROJECT_ROOT is normally injected by rootutils and can also be set in .env
root_dir: ${oc.env:PROJECT_ROOT}

# optional data anchors loaded from .env
data_root: ${oc.env:DATA_ROOT,${paths.root_dir}/data}
raw_data_dir: ${oc.env:RAW_DATA_DIR,${paths.data_root}/raw}
preprocessed_data_dir: ${oc.env:PREPROCESSED_DATA_DIR,${paths.data_root}/preprocessed}

# backward-compatible alias for preprocessed data
data_dir: ${paths.preprocessed_data_dir}

# path to logging directory
log_dir: ${paths.root_dir}/logs/

# path to output directory, created dynamically by hydra
# path generation pattern is specified in `configs/hydra/default.yaml`
# use it to store all files generated during the run, like ckpts and metrics
output_dir: ${hydra:runtime.output_dir}

# path to working directory
work_dir: ${hydra:runtime.cwd}
